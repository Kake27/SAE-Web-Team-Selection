# For SAE Web Team Selection

Clone of the provided Figma design made mainly using React.js (along with Vite).
To run locally, clone the repo, install the required packages using npm and run npm run dev.
